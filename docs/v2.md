# v2 — notes parked during the design-24 verification walk (2026-07-26)

One line each; none is a design-24 failure. Aaron rules on all of these.

## Resolved (docs synced 2026-07-26, no code change)

- The 2026-07-26 amendments (outro split into steps 6–7, epilogue SpeedRace
  panel, chat streaming/reasoning toggle, design-33 expert adoption, moment-
  carrying view links) are now in storyboard.md + copy-script.md.
- The stale step-5 "at scale" dock handle is retired from copy-script.md's
  addendum; the outro's real mechanism (continue path, hollow rail dots) is
  documented in storyboard.md.
- The design-31 instrument strings that predated the addendum's I-slot
  inventory (LoopChain, DrawField, TokenSpace corner/read/honest lines, plus
  LensSpace's new corner readout) are now listed.

## Still open — genuinely optional, awaiting a call

- Experiment hooks surface "attention" before its step-2 ledger row: the
  front-door button tooltips (step 0) and the step-1 experiment mark ("watch
  attention fetch a fact"). Left untouched — experiments.ts is curated copy
  outside CC's editing authority per copy-script.md's own contract.
- docs/finish-line.md (named by the audit session's prompt) does not exist in
  the tree; the walk proceeded on design-24 + storyboard + copy-script
  directly.
- design-33's pitched-alongside items: a plain-language register pass on the
  expert band subtitles (SUB.* in Explanations.tsx), and entrance-stagger
  timing on the Logits bars. Not done in the 2026-07-26 finishing pass:
  design-33 itself states "the expert register stays its own" as a
  deliberate distinction from the tour's beginner register, so a rewrite
  toward the tour's voice would reverse a decision already made, not just
  polish it. Left for an explicit call.
